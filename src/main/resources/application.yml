spring:
  application:
    name: basket-service

  mongodb:
    host: localhost
    port: 27017
    database: basket-service
  data:
    redis:
      host: localhost
      port: 6379
      password: sa
  cache:
    redis:
      time-to-live: 60000

basket:
  client:
    platzi: https://api.escuelajs.co/api/v1

springdoc:
  api-docs:
    path: /api/api-docs
  swagger-ui:
    path: /swagger/index.html